<!DOCTYPE html>
<html>
<head>
	<title>Quotes</title>
</head>
<body>
	<h1>Here are the awesome quotes!!!</h1>
	<div>
	<% for (let quote in quotes) {%>
		<h3><%=quotes[quote].quote%></h3>
		<p>- <%=quotes[quote].name%> created at <%=quotes[quote].createdAt%></p>
	<%}%>
	</div>
	<button><a href="/">Go Back</a></button>
	<% if(typeof(errors) != 'undefined' ) { %>
    	<% for (var x in errors) { %>
    		<h3><%= errors[x].message %></h3>
    	<% } %>
	<% } %>


</body>
</html>